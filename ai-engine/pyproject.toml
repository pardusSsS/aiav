[project]
name = "aiav-engine"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "onnxruntime>=1.18",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.7"
]


[tool.setuptools]
packages = ["ai_engine"]